---
import Layout from "../../layouts/Layout.astro";
import PriceCard from "../../components/PriceCard.astro";
import { getCollection } from "astro:content";
const products = await getCollection('products');
---

<Layout title="Prices - PaztG Design">
	<main class="max-w-6xl mx-auto flex flex-col gap-5">
		<div class="text-center flex flex-col gap-3">
			<h1 class="text-3xl font-bold uppercase">Prices</h1>
			<p class="text-xs lg:text-sm px-5">I cannot provide a fixed price as it depends on the specifics of your idea and our final discussion. However, I've created a price table to simplify the process of determining costs for you.</p>
		</div>
		<div class="grid lg:grid-cols-3 gap-5 p-5 border-white/20 rounded-md">
			{
				products.map(async (product) => (
					<PriceCard name={product.data.name} estimatedTime={product.data.estimatedTime} price={product.data.price}/>
				))
			}
		</div>
	</main>
</Layout>
